// Header.js
import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.header`
    font-size: 24px;
    margin-bottom: 20px;
`;

const Header = () => (
    <StyledHeader>2ul.top - URL Shortener</StyledHeader>
);

export default Header;
